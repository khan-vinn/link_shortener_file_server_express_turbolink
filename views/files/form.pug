extends ../layout.pug
block append head
    title File service

block content
    .file-upload 
        form(action="/f" method="post" enctype="multipart/form-data")
            label(for="file_upload") Select file
            input#file_upload(type="file" name="file" required)
            input(type="submit" value="Submit")
